import React, {useEffect} from 'react';
import {
    Button,
    Box
} from '@material-ui/core'


function Home() {
   

    return (
        <div>
            <Button variant='contained'>Register your company</Button>
            <Button variant='contained'>Login</Button>
            <Button variant='contained'>View Demo Version</Button>
        </div>
        );
}

export default Home;
